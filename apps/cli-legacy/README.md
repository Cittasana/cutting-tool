# Cutting Tool — CLI (Legacy v1)

> Wird in v2 abgelöst. Dieser Code wird Schritt für Schritt nach `apps/web` und `packages/core` migriert.
> Keine neuen Features hier. Nur Bug-Fixes wenn der CLI-Pfad noch gebraucht wird.

Original-README siehe Git-History (v0.1.0).
